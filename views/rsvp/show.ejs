<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Attendee</title>
</head>
<body>
      
<h1>Attendee</h1>

<form action="/rsvp" method="POST">
    <main>
        <div class="attendee-details">
            <label for="firstName">First Name:</label>
            <input type="text" id="firstName" name="firstName" value="<%= attendee.firstName %>" disabled>
    
            <label for="lastName">Last Name:</label>
            <input type="text" id="lastName" name="lastName" value="<%= attendee.lastName %>" disabled>
    
            <label for="plusOne">Plus One:</label>
            <input type="text" id="plusOne" name="plusOne" value="<%= attendee.plusOne %>" disabled>
        </div>
    </main>

    <div class="buttons">
        <% if (user.isAdmin) { %>
            <button class="edit-button">Edit</button>
            <button class="delete-button">Delete</button>
        <% } %>
    </div>
</form>

</body>
</html>
